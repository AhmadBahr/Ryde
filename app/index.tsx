import { View, Text } from "react-native";
import { SafeAreaContext } from "react-native-safe-area-context";

const Home = () => {
    return (
        <SafeAreaContext>
            <Text>Home</Text>
        </SafeAreaContext>
    )
}
export default Home;